""" Tuples
- are immutable (we can not append, remove, replace)
The only way to change the content of a tuple is `declare it again"""

x = ('pop', 3.123, 'zef', 5, not True)
# cause an error
# x[2] = 'sadlkfjlkdsfahj'

print(x[2])
